# 都立公園スポーツ施設 空き状況監視ツール — プロジェクトノート

最終更新: 2026-08-14　ステータス: **動作確認済み**（木場公園テニス人工芝の実データ取得に成功）

このドキュメントは、今後このプロジェクトを再開・拡張するときに経緯を思い出すためのものです。
コード自体のコメントより詳しい「なぜこうなったか」をここに残します。

---

## 1. 何を作っているか

東京都立公園スポーツ施設予約システム（`kouen.sports.metro.tokyo.lg.jp`）の空き状況を
定期的（1時間ごと想定）にチェックし、**新しく空きが出た瞬間を検知する**ツール。

[[ur-housing-monitor]]（UR賃貸監視）プロジェクトと同じ設計思想。あちらは「新着物件の検知」、
こちらは「予約キャンセル等で発生した新規空きコマの検知」。

監視範囲は最初から意図的に絞ってある：**よく行く具体的な公園・場地のみ**。
UR賃貸監視のような「23区全域を継続的に発見していく」タイプの設計ではない。

現在の監視対象（`monitor.js`内 `TARGETS` 配列、計4施設）：

| 施設名 | areaCd_bldCd | instCd | 種目(purpose) |
|---|---|---|---|
| 木場公園 テニス（人工芝） | 108_1060 | 10600010 | 1000_1030 |
| 有明テニスＡ屋外ハードコート | 108_1350 | 13500010 | 1000_1020 |
| 有明テニスＢインドアコート | 108_1370 | 13700010 | 1000_1020 |
| 有明テニスＣ人工芝コート | 108_1360 | 13600010 | 1000_1030 |

いずれも「こだわり検索」カタログページの該当施設ボタン（`doSearch(...)`）から
コードを直接読み取ったもの。すべて江東区（areaCd=108）。

複数施設を監視したくなったら、`TARGETS`配列にオブジェクトを追加するだけでよい設計にしてある
（`areaCd`/`bldCd`/`instCd`/`purpose`の4点セットが必要）。

---

## 2. 最大の発見：ログイン不要だった

このシステムは当初、`userId`+`password`でのログインが必須だと思って実装していた
（ログインページのHTML解析、`loginJKey`トークンの取得、パスワードを1文字ずつ分割して送る
`loginCharPass`という奇妙な仕組みまで解析して実装した）。

**しかし実際には、「こだわり検索」という画面経由でアクセスすると、ログインなしで
空き状況を閲覧できる**ことが実測で判明した。ログイン関連のコードはすべて撤去済み。

なぜ気づけたか：ブラウザで実際に操作しながら、こだわり検索画面（未ログイン状態のまま）
から空き状況ページに到達できることをユーザーが発見した。

---

## 3. 動作確認済みの処理フロー

実際にブラウザの通信をキャプチャして特定した、正確な4ステップ：

| # | メソッド | URL | 役割 |
|---|---|---|---|
| 1 | GET | `/web/index.jsp` | セッション確立（JSESSIONID取得） |
| 2 | POST | `/web/rsvWTranceKodawariAction.do` | 「こだわり検索」ボタン押下 → 全公園カタログページ取得 |
| 3 | POST | `/web/rsvWOpeKodawariSearchAction.do` | 個別公園の「空き検索」ボタン押下 → 空き状況ページへ遷移 |
| 4 | POST | `/web/rsvWOpeInstSrchVacantAjaxAction.do` | 実際の空き状況データ（JSON）取得 |

**Step1・Step2は画面遷移のみでAjaxは発生しない。Step3が実際にデータベースへ
アクセスする分岐点で、これを省略すると`{"ErrManager":{"message":"システム異常が
発生しました。"}}`というエラーが返る。** これはユーザーが実機検証で確定させた事実。

### 3-1. 各ステップの詳細

**Step2（こだわり検索カタログ取得）** — パラメータは完全に実測済み。
ホーム画面(`displayNo=pawab2000`)時点のform1のデフォルト状態（何も選択していない状態）を
そのままPOSTするだけでよい。`daystart`/`daystarthome`は当日日付（`YYYY-MM-DD`形式）、
残りはほぼ固定値または空文字。

**Step3（個別公園の空き検索）** — ★まだ100%実測できていない部分。
ブラウザ上でのボタンクリックは `doSearch(document.form1, gRsvWOpeKodawariSearchAction,
'108_1060', '10600010', '1000_1030')` というJS関数呼び出しで、これが
`rsvWOpeKodawariSearchAction.do`へPOSTする**はず**だが、そのPOSTボディの実際の
フィールド名は一度もキャプチャできていない（何度か「ブラウザで空き検索ボタンを押した
瞬間の通信を見せてほしい」とお願いしたが、毎回別の通信を渡されてしまい実測に至らなかった）。

現在の実装は、ログインフォームで実測済みの命名規則
（`selectAreaBcd`/`selectBldCd`/`selectIcd`/`selectPpsClsCd`/`selectPpsCd`）を
類推で流用している。**これで実際に正しい結果ページに到達することは確認済み**
（後述4-2参照）なので、動作上は問題ないが、正式なフィールド名ではない可能性がある。

**Step4（空き状況AJAX）** — 完全に実測済み。`bldCd`+`instCd`+`useDay`を渡すと、
その週（7日間）の時間帯ごとの空き状況が返る。

---

## 4. ハマったポイントと解決の経緯（時系列）

### 4-1. Shift_JISの文字化け
このサイトはHTML/AJAXレスポンスとも`charset="Shift_JIS"`。Node.jsの`res.text()`は
UTF-8前提でデコードするため、日本語部分が文字化けする。`res.arrayBuffer()` +
`TextDecoder("shift_jis")`で明示的にデコードする対応で解決（`readAsShiftJis()`関数）。

### 4-2. 「システム異常」エラー → Step2/Step3の発見
ログイン撤去直後、Step3・Step4だけを呼ぶ実装にしたところ
`{"ErrManager":{"message":"システム異常が発生しました。"}}`が返った。
原因調査の過程で、実際のブラウザ操作は
「ホーム画面 → こだわり検索カタログ → 個別公園の空き検索」という3段階のページ遷移を
経ていることが判明し、これをStep1〜3として再現することで解決した。

### 4-3. 施設一覧が0件で返る問題 → 誤った仮説の除去
Step2・3を追加した直後、エラーは解消したが `rsvWOpeInstSrchVacantBuildAjaxAction.do`
（施設一覧取得）が`{"results":[]}`という空配列を返す問題が発生。

一時的に「ブラウザのCookieに`bname`/`purpose`/`daystart`/`collapseWhen`/`collapseWhere`
というクライアント側JS設定のCookieが必要なのでは」という仮説を立てて実装したが、
ユーザーが提供した実際の成功リクエストのCookieには`KIOSK`/`JSESSIONID`/`FJNADDSPID`
しか含まれていないことが判明し、この仮説は誤りと判明→撤去した。

### 4-4. 施設一覧APIは不要だと判明
Step3の到達先ページ（`prwrc2000.jsp`、空き状況画面）を実測したところ、
ページ内の隠しフィールド`initBcd="108"`・`initIcd="10600010"`が正しく
木場公園のテニス人工芝を指していることが確認できた。

**つまり`instCd`は「こだわり検索」カタログのボタンから最初から分かっているので、
施設一覧AJAX（`rsvWOpeInstSrchVacantBuildAjaxAction.do`）で動的に調べる必要が
そもそも無かった**。このAPI呼び出しを削除し、`TARGETS`に書いてある`instCd`を
直接使ってStep4を呼ぶようにしたところ、実際のデータ取得に成功した。

（`getFacilities()`関数はコード上には残っているが、現在は未使用。将来
「1つの公園に複数施設があり、動的に列挙したい」場合には使える）

---

## 5. まだ確定していないこと / 次にやるべきこと

1. **Step3の正式なPOSTボディが未実測。** 動作はしているが、なぜ動くのか
   完全には理解できていない。ブラウザで「空き検索」ボタンを押した**瞬間**の
   リクエスト（Sec-Fetch-Dest: document、method: POST、URL末尾が
   `rsvWOpeKodawariSearchAction.do`）をまだ一度もキャプチャできていない。
   将来動かなくなった場合、まずここを疑うこと。

2. **通知連携が未実装。** `main()`内に`// TODO: ここで通知連携`とコメントだけ
   置いてある。LINE Notify、Slack Webhook、メール等、どれを使うか未検討。

3. **GitHub Actionsでの本番運用がまだ試されていない。**
   `.github/workflows/monitor.yml`は用意済み（毎時cron実行、`npm install`→
   `node monitor.js`→ `data/snapshot.json`と`data/alerts.log`を自動コミット）だが、
   実際にリポジトリにpushして動かした実績はまだない。ログイン不要になったため
   GitHub Secretsの設定は不要。

4. **複数施設・複数公園への拡張は未検証。** `TARGETS`配列に2件目を足したときに
   問題なく動くかは未確認（理論上は動くはず）。

---

## 6. ファイル構成

```
kouen-monitor/
├── monitor.js              本体スクリプト（このファイルの詳細な処理コメント参照）
├── package.json             依存関係（dotenvのみ）
├── .env.example              環境変数テンプレート（WEEKS_AHEAD, TARGET_DATE）
├── .gitignore
├── PROJECT_NOTES.md          このファイル
├── .github/workflows/
│   └── monitor.yml           GitHub Actions cron設定（毎時実行、未検証）
└── data/
    ├── snapshot.json         前回実行時の空き状況スナップショット（diff用）
    ├── alerts.log             新規空き検知のログ（追記式）
    └── debug_*.html/json      --debug オプション実行時のみ生成されるデバッグ出力
```

## 7. よく使うコマンド

```bash
npm install              # 初回のみ
node monitor.js          # 通常実行
node monitor.js --debug  # data/debug_*にレスポンスを保存しながら実行（トラブル時用）
```

環境変数（`.env`ファイルまたは実行時指定、どちらも省略可）：
- `WEEKS_AHEAD` : 何週間先まで見るか（デフォルト1 = 直近7日間のみ）
- `TARGET_DATE` : 検索開始日 YYYYMMDD（デフォルトは動作確認用に固定値。
  実運用では当日日付に変えるのが自然）